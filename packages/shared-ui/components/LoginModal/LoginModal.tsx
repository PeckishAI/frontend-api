import './style.scss';

type Props = {
  isVisible: boolean;
};

const LoginModal = (props: Props) => {
  return (
    <div className="modal">
      <input></input>
      <input></input>
      <button></button>
    </div>
  );
};

export default LoginModal;
